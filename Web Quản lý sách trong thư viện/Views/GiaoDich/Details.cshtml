@model Quản_Lý_Sách.Models.GiaoDich

@{
    ViewBag.Title = "Chi tiết giao dịch";
}

<h2>Chi tiết giao dịch</h2>

<div>
    <h4>@Model.MaGD</h4>
    <hr />
    <dl class="dl-horizontal">
        <dt>MaKH</dt>
        <dd>@Model.MaKH</dd>
        <dt>MaSach</dt>
        <dd>@Model.MaSach</dd>
        <dt>NgayMuon</dt>
        <dd>@Model.NgayMuon?.ToString("dd/MM/yyyy")</dd>
        <dt>NgayTraDuKien</dt>
        <dd>@Model.NgayTraDuKien?.ToString("dd/MM/yyyy")</dd>
        <dt>NgayTraThucTe</dt>
        <dd>@Model.NgayTraThucTe?.ToString("dd/MM/yyyy")</dd>
    </dl>
</div>

<div>
    @Html.ActionLink("Sửa", "Edit", new { id = Model.MaGD }) |
    @Html.ActionLink("Quay lại", "Index")
</div>
